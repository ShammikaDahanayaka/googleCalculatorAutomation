{
  "name": "initial-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress:open": "cypress open",
    "test:e2e": "cypress run --browser chrome",
    "test:e2e:smoke": "cypress run --browser chrome --spec 'cypress/integration/smoke/*'",
    "test:e2e:functional": "cypress run --browser chrome --spec 'cypress/integration/functional/*'"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.9.0",
    "cypress-mochawesome-reporter": "^3.8.2",
    "cypress-xpath": "^2.0.1",
    "mochawesome": "^7.1.3"
  }
}
